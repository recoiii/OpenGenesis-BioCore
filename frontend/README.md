# OpenGenesis-BioCore Browser Frontend

OpenGenesis-BioCore uses a local-only HTML/CSS/Vanilla JavaScript shell. The frontend has no CDN or runtime network dependency. It performs only the public same-origin `/api/v1/health` check.
